# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
# Cualquier variante, no solo las cuatro de CRA: durante una rotacion, una
# copia .env.local.bak con credenciales dentro casi acaba en el repo.
.env*
!.env.example
/data/local-users.json

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Exports temporales de ofertas (generados por scripts, no versionar)
/ofertas-mercado-*.csv
/ofertas-mercado-*.xlsx
# Fichero basura de redirects Windows (> nul)
nul

# Datos OSM crudos (>1 GB, no versionables)
*.pbf

# Archivos lock de Office (documento abierto)
~$*

# Logs y temporales de scripts
tmp_*.log

# Configuración local de Claude Code
.claude/settings.local.json

# Directorios ERP con node_modules propios
carswise-erp-backoffice/node_modules/
carswise-erp-backoffice-review/

# Vercel: el enlace local con el proyecto, que crea `vercel link`.
.vercel

# Correos del ensayo de los cron: se regeneran solos.
scripts/ensayo-correos/

# La copia suelta en la raiz: ya vive en public/ y en mockups/assets/.
# Se quito una vez a proposito y un `git add -A` la devolvio.
/Beetle PopCar.png
